# Claude Code 引き継ぎガイド

## このプロジェクトについて

オートレースの公開出走表・試走・確定結果を取得し、3連単の確率を計算するWebアプリです。
画面を閉じている間もDeno DeployのCronが15分ごとに収集・結果照合・PDCAを実行します。

- 公開URL: https://autorace-yosou-cloud.hakutsu810-web.deno.net/
- GitHub: `hakutsu810-web/autorace-yosou-cloud`
- Deno Entrypoint: `deno/main.mjs`
- 主なデータ元: WINTICKETの公開ページ
- 予想モデル: Plackett-Luce
- 現在のモデル版: v3

## 最初に読むファイル

1. `index.html` — 単一HTMLの画面とブラウザ側予想
2. `netlify/functions/lib/model.mjs` — サーバー側の予想・学習
3. `netlify/functions/lib/source.mjs` — WINTICKET取得・解析
4. `cloudflare/worker.mjs` — 収集・結果照合・過去データ追加
5. `cloudflare/state.mjs` — Deno KVの状態形式・モデル移行
6. `deno/main.mjs` — Deno HTTP/API/Cron/KV

`netlify/functions/lib/store.mjs` はNetlify用の永続化です。現在の本番はDeno Deployですが、モデル版はDeno側と揃えてください。

## 絶対に守る仕様

- 6車・7車・8車を実際の出走車数で処理する。
- 3連単の通り数は6車120、7車210、8車336。
- Plackett-Luce列挙後の確率合計を1にする。
- 欠車や空欄の車をモデルへ入れない。
- 結果確定後の情報をレース前予想へ混入させない。
- 学習は保存済みのレース前スナップショットと確定結果だけで行う。
- 固定の根拠不明な「全国統計」を表示しない。蓄積結果は母数付きで表示する。
- 的中保証・必勝・絶対という表現を使わない。
- 舟券ではなくオートレースの車券であり、20歳以上・余裕資金の注意書きを残す。

## 現在の精度改善方針（v3）

- 試走係数 `k` の初期値は `0.40`。
- 学習候補は `0.25 / 0.35 / 0.40 / 0.45 / 0.55`。試走だけへ過度に寄せない。
- `試走T - 平均試走T` は `±0.06秒` でクリップする。
- 全国ランク、審査点、級別、近況、良・湿3連対率、ST、ハンデを併用する。
- モデル変更時は `cloudflare/state.mjs` と `netlify/functions/lib/store.mjs` のバージョン移行を揃える。
- 既存の検証成績と新モデルの成績を混ぜず、モデル版変更時はPDCAの直近評価を分ける。

## 変更時の検算

最低限、次を確認してください。

1. 6車の3連単が120通りで確率合計1。
2. 7車が210通りで確率合計1。
3. 8車が336通りで確率合計1。
4. 1着確率の合計が1。
5. 山陽などの6車立てで7・8号車の空欄が混入しない。
6. 「本日の予想・結果」で予想、確定結果、配当、モデル順位が同じ行に出る。
7. 「実績統計」に集計母数と6/7/8車の内訳が出る。
8. ブラウザのコンソールエラーがない。
9. Deno Deployの最新ビルドがSuccessになる。

## データ取得に関する注意

WINTICKETのHTML構造変更で解析が壊れる可能性があります。取得できない場合は、まず保存済みHTMLや対象ページのセル構造を確認し、`source.mjs` のパーサだけを小さく直してください。取得失敗を0や架空値で埋めないでください。

過去5年分は一度に大量取得せず、Cronごとに少量ずつ追加します。アクセス負荷と利用条件に配慮してください。

## 公開方法

GitHubの`main`へ反映するとDeno Deployが自動ビルドします。公開前に差分と検算結果を説明し、データ形式を変更した場合は移行処理を必ず含めてください。

## Claude Codeへの依頼例

`CLAUDE-CODE-PROMPT.txt`をそのまま貼り付け、末尾に変更したい内容を追加してください。

